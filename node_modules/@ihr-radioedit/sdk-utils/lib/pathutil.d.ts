/**
 * Ensure that a path has no trailing separator.
 */
export declare function removeTrailingSep(path: string, removeRoot?: boolean): string;
/**
 * Ensure that a path has a leading separator.
 */
export declare function addLeadingSep(path: string): string;
/**
 * Ensure that a path has no leading separator.
 */
export declare function removeLeadingSep(path: string): string;
/**
 * Path join (dedupes separators, etc).
 */
export declare function joinPath(...parts: string[]): string;
/**
 * Create a slug from an arbitrary string.
 * Untouched characters are alpha-numeric, dash (unless consecutive), and underscore.
 */
export declare function slugify(s: string): string;
//# sourceMappingURL=pathutil.d.ts.map