import { ErrorBase, registerErrorDeserializer } from "@ihr-radioedit/errors";
/**
 * Error thrown when the caller passes invalid options to the adapter. What constitutes invalid options
 * is adapter-specific.
 */
export class InvalidOptions extends ErrorBase {
    code = 400;
    static fromErrorData(data) {
        const err = new InvalidOptions(data.message);
        err.stack = data.stack;
        return err;
    }
    toErrorData() {
        const { name, stack, message, code } = this;
        return { name, stack, message, code };
    }
}
registerErrorDeserializer("InvalidOptions", InvalidOptions.fromErrorData);
//# sourceMappingURL=invalid-options.js.map