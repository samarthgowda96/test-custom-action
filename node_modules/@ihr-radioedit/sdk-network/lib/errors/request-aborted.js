import { NetworkError } from "./base.js";
/**
 * Error type for request timeouts and other abort conditions.
 */
export class RequestAbortedError extends NetworkError {
    static fromResponse(resp, message) {
        const err = new RequestAbortedError(message ?? resp.statusText);
        err.response = resp;
        return err;
    }
}
//# sourceMappingURL=request-aborted.js.map