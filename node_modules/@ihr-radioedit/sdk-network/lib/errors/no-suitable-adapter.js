import { ErrorBase, registerErrorDeserializer } from "@ihr-radioedit/errors";
/**
 * Error thrown when trying to fetch an adapter class that's not registered, and no default adapter is available.
 */
export class NoSuitableAdapter extends ErrorBase {
    code = "NO_SUITABLE_ADAPTER";
    static fromErrorData(data) {
        const err = new NoSuitableAdapter(data.message);
        err.stack = data.stack;
        return err;
    }
    toErrorData() {
        return {
            name: this.name,
            message: this.message,
            stack: this.stack,
            code: this.code,
        };
    }
}
registerErrorDeserializer("NoSuitableAdapter", NoSuitableAdapter.fromErrorData);
//# sourceMappingURL=no-suitable-adapter.js.map