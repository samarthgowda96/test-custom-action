import { ErrorBase, ErrorData } from "@ihr-radioedit/errors";
/**
 * Error thrown when the caller passes invalid options to the adapter. What constitutes invalid options
 * is adapter-specific.
 */
export declare class InvalidOptions extends ErrorBase<never> {
    code: number;
    static fromErrorData(data: ErrorData<any>): InvalidOptions;
    toErrorData(): {
        name: string;
        stack: string | undefined;
        message: string;
        code: number;
    };
}
//# sourceMappingURL=invalid-options.d.ts.map